# Core data
datasets>=2.19
pandas>=2.2
pyarrow>=16
numpy>=1.26

# Retrieval / storage
rank_bm25>=0.2.2
datasketch>=1.6
psycopg[binary]>=3.1
pgvector>=0.3

# OpenRouter (generator, judge, embeddings, rerank — one key)
openai>=1.35
python-dotenv>=1.0

# Exact budget tokenization: the generator's own tokenizer (Qwen3)
transformers>=4.51
huggingface_hub>=0.23

# Compression arm (LLMLingua-2); documented fallback in arms/compress.py.
# torch is a transitive dep of llmlingua but arms/compress.py imports it
# directly, so it is declared explicitly.
llmlingua>=0.2.2
torch>=2.0

# Graph arm NER (model installed as a direct wheel: uv venvs have no pip, so
# spaCy's runtime auto-download cannot work; regex fallback in src/04_arms.py
# only if spaCy itself is unavailable)
spacy>=3.7
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Analysis
matplotlib>=3.8
scipy>=1.13
statsmodels>=0.14
requests>=2.31
tabulate>=0.9          # pandas.to_markdown in 08_summary

# Exact versions used for the recorded runs: requirements.lock.txt
