# --- pre-existing local files (not part of the published repo) ---
CLAUDE_CODE_PROMPT.md
INTERIM_REPORT_MAP.md
plan_*.json
QM 640 Interim Report template-1.docx
*.docx

# --- secrets ---
.env

# --- data & caches (outputs/ IS committed — the report cites it) ---
# data/* (not data/) so the negation below can re-include the small sample:
# git never descends into a fully-excluded directory
data/*
!data/sample/
!data/sample/**
.cache/
llm_cache/
*.parquet
!data/sample/*.parquet

# --- python ---
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# --- os ---
.DS_Store

# --- colab transfer bundle (large, regenerable) ---
colab_bundle.zip
